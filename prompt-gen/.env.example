ANTHROPIC_API_KEY=sk-ant-...
# optional:
OPENAI_API_KEY=
EMBED_MODEL=text-embedding-3-small
EMBED_THRESHOLD=0.68
# Storage. Locally this defaults to ./strategy_bot.db. On Railway, point it at a
# mounted volume so data survives redeploys, e.g. DB_PATH=/data/strategy_bot.db
# DB_PATH=/data/strategy_bot.db
